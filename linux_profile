#!/usr/bin/env python3

from setuptools import setup
from core.commands import CommandInit, CommandAdd, CommandSync


setup(
        name="linux-profile",
        cmdclass={
                'init': CommandInit,
                'add': CommandAdd,
                'sync': CommandSync
            }
    )
