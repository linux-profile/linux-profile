#!/usr/bin/env python3

from setuptools import setup
from core.command import Init, Add, Sync


setup(
        name="linux-profile",
        cmdclass={
                'init': Init,
                'add': Add,
                'sync': Sync
            }
    )
